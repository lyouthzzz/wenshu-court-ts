{
    "files.exclude": {
        "node_modules/": true,
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.d.ts": {
            "when": "$(basename).ts"
        },
        "**/*.js.map": true
    }
}